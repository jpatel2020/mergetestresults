{
  "name": "mergetestresults",
  "version": "1.0.0",
  "description": "Browser level test results(JSON formatted) are aggregated into feature level test results (JSON formatted) ",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jateen Patel",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-google": "^0.14.0"
  }
}
